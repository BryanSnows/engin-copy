const default_value = 100;

export const Profile = {
  CREATE_PROFILE: [default_value],
  CHANGE_PROFILE: [default_value],
  READ_PROFILES: [default_value],
  READ_PROFILES_BY_ID: [default_value],
  READ_TRANSACTIONS: [default_value],
  CHANGE_TRANSACTION_STATUS_BY_ID: [default_value],
};
